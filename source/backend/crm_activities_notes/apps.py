from django.apps import AppConfig

class CrmActivitiesNotesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'crm_activities_notes'
    verbose_name = 'CRM Activities Notes'
