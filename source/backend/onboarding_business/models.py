# backend/onboarding_business/models.py

# Empty - on utilise les models existants
# Company → company_core.models
# Brand → brands_core.models  
# CustomUser → users_core.models
# CompanySlots → company_slots.models
# CompanyFeature → company_features.models
# UserRole → users_roles.models